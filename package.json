{
  "name": "fullstack-atlas-react-laravel",
  "private": true,
  "scripts": {
    "dev": "concurrently -n FRONTEND,BACKEND -c blue,green \"npm run dev --prefix frontend\" \"php -S 127.0.0.1:8000 -t backend/public\"",
    "dev:artisan": "concurrently -n FRONTEND,BACKEND -c blue,green \"npm run dev --prefix frontend\" \"php backend/artisan serve --host=127.0.0.1 --port=8000\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
